Converts a .blob/.bin into a formatted python script.

this outputted py.script can then be edited and put into the stmserver folder inwhich the server will load firstblob.py/secdonblob.py instead of the .bin versions.
